const cars: string[] = ['toyota', 'marcedes'];
const dates: Date[] = [new Date(), new Date()];

// 2D array
const carByMake: string[][] = [['camero'], ['toyolo'], ['corola']];
let bigArray: number[][];

cars.map((car): string => {
  return car;
})

const greatTImes: (string | Date)[] = [new Date(), 'saturday'];